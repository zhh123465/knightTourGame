# Maven
target/
dependency-reduced-pom.xml

# IDEs
.idea/
*.iml
.vscode/
.settings/
.classpath
.project
*.launch

# Logs
*.log
logs/

# OS generated files
.DS_Store
Thumbs.db

# Other
*.class
*.jar
*.war
*.ear
*.zip
*.tar.gz
*.rar

# Keep the release folder but ignore content if large, but user wants to push source.
# Usually we don't push binaries to git source repo.
# Releases should be uploaded to GitHub Releases.
# So we ignore the installer output in target, which is already covered by target/

# Kiro specific
.kiro/
